@model CalculatorModel
@{
    ViewData["Title"] = "Calculator Result";
}

<h2>CalculatorResult</h2>

<p>You typed in <strong>@Model.NumberToConvert @Model.StartingUnit</strong>. When
    converted to <strong>@Model.EndingUnit</strong>, it is
    <strong>@Model.GetConvertedValue()  @Model.EndingUnit</strong></p>